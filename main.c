/* int main(int argc, char **argv, char **env)
{
        if (argc < 5)
                return (ft_putstr_fd("./pipex file1 cmd1 cmd2 ... file2\n", 2), 1);
//      if (ft_strncmp(argv[1] == "here_doc"))
//      {
//              if (argc < 6)
//                      return (ft_putstr_fd("./pipex here_doc LIMITER cmd1 cmd2 ... file2\n")1);
//              else
//                      here_doc(argv, env);
//      }
//      else
        pipex(argv, env);
        return (0);
}

 */